package org.alksndrstjc.service;

import junit.framework.TestCase;
import org.junit.Test;

import java.io.IOException;

public class RequestMakerTest extends TestCase {

    @Test
    public void testMockResponse() throws IOException, InterruptedException {
//        HttpClient mockClient = Mockito.mock(HttpClient.class);
//        HttpClient mockedClient = Mockito.mock(HttpClient.class);
//        HttpRequest mockedRequest = Mockito.mock(HttpRequest.class);
//        HttpResponse<String> mockedResponse = Mockito.mock(HttpResponse.class);
//
//        Mockito.when(mockedClient.send(mockedRequest, Mockito.any(HttpResponse.BodyHandler.class)))
//                .thenReturn(mockedResponse);
//        Mockito.when(mockedResponse.statusCode()).thenReturn(200);
//        Mockito.when(mockedResponse.body()).thenReturn("jes mi ga");
//
//        // Replace the actual HttpClient with the mocked one
//        RequestMaker maker = new RequestMaker(mockClient);
//        HttpResponse<String> response = maker.doRequest(mockedRequest);
//
//        assertEquals(200, response.statusCode());
//        assertEquals("jes mi ga", response.body());

    }

}